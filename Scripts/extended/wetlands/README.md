# Coastal wetlands (Sentinel / GEE)

- **Notebook:** `notebooks/1-humedales_descarga_sentinel_v2.ipynb`
- **Python modules:** `python/` (`functions.py`, `download_gee.py`, `gee_landsat.py`, `planet_estuary_small.py`)
- **Geometries:** `Input/extended/wetlands/shapes/ligua_petorca_coast/*.geojson`

Requires Google Earth Engine and local Python packages (see parent `Scripts/extended/README.md`). Edit hardcoded paths inside notebooks/scripts if they still point to a colleague machine.
